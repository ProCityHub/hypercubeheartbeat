# Cognitive Cycle Planbook Refresh Runbook
> **Post-reconciliation name:** The active audit-only implementation is
> `tools/audit_planbook.py`. The former
> `tools/audit_planbook.py` path was removed during repository
> reconciliation. The audit-only implementation is `tools/audit_planbook.py`
> and must not be used as the conversational interface.

## Status

Implementation runbook for DIRECTIVE-008E.

This is a Stage 2 cognitive planbook refresh.

It is not a new action engine.

It is not a memory database implementation.

## Tool updated

`tools/audit_planbook.py`

## Purpose

The Cognitive Cycle Runner previously recommended organs that had already been merged.

DIRECTIVE-008E refreshes the runner's internal body map and candidate planbook so Heartbeat reasons from the current GARVIS body.

## Updated known organs

The runner now recognizes:

- cognitive cycle runner
- cognitive cycle viewer
- cognitive cycle memory ledger contract
- cognitive cycle memory ledger record schema

## Updated candidate planbook

The runner now proposes newer next steps:

1. Cognitive Cycle Memory Ledger Init CLI
2. Cognitive Cycle Memory Append CLI
3. Power Request Queue Contract

## Why this matters

A thinking system must know when its map is stale.

The 008D heartbeat still recommended 008C even though 008C was already merged.

That means the next correct organ is not more power.

The next correct organ is better self-knowledge.

## Stage classification

This is Stage 2 cognitive draft infrastructure.

It refreshes candidate thought generation.

It creates no database.

It writes no memory.

It executes no candidate.

## No-network law

This refresh creates no network calls.

It creates no LLM calls.

It creates no outside contact.

## Standing boundary

A planbook is a map.

A map is not movement.

A recommendation is not approval.

Adrien decides.
